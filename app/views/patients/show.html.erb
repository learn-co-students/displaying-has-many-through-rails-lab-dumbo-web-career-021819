<h1> <%=@patient.name %></h1>

<h2> Apppointments: </h2>
  <ul>
    <% @patient.appointments.each do |app|%>
    <li>Time: <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> |
      Doctor: <%=link_to app.doctor.name, doctor_path(app.doctor)%></li>
    <%end%>
  </ul>
