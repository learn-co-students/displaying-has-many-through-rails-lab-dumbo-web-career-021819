<%= link_to @appointment.patient.name, "/patients/#{@appointment.patient_id}" %><br>
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>
<%= link_to @appointment.doctor.name, "/doctors/#{@appointment.doctor_id}"%>
